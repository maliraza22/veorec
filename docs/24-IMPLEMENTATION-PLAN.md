# 24 — Implementation Plan

> The migration plan (`23`) broken into small, sequential tasks an engineer (or Claude Code session) can execute one at a time. Task format: **ID · Title** — Objective / Deps / Files / Details / Tests / Acceptance. IDs are stable; reference them in commits (`T-103: ...`). Do not start a task whose deps aren't merged.

Repo layout target (created incrementally):
```
/apps/api  /apps/worker  /apps/web (moved client/)  /apps/extension (moved extension/)
/packages/shared (types, error codes, plan catalog)  /db (drizzle schema + migrations)
/docs  /tests
```

---

## Phase 0 — Freeze & baseline

**T-001 · Repo hygiene & secrets** — ✅ DONE (2026-09-01). Untracked the 10 stale release zips (≤v1.7.3); verified `extension.pem`/`.crx` were never in git history (no rotation needed); imported the `docs/` package into the repo (canonical copy) + root `README.md`; established the verified baseline in `docs/BASELINE.md`. Accept met: `git ls-files` clean of `.zip/.crx/.pem`.

**T-002 · Structured logging on legacy server** — ✅ DONE (2026-09-01). `server/log.js` (pino 10 + pino-http 11 + optional @sentry/node 10) wired into `server/index.js` top-level only: X-Request-Id in/out (inbound accepted only when it matches `^[A-Za-z0-9._-]{6,64}$`, else `req_<uuid>` generated), one structured completion line per request (`request_id`, allowlisted `req{method,url-sans-query}`, `res.status`, `responseTime`), error middleware captures to Sentry with request-id tags, `uncaughtExceptionMonitor` observes fatals without changing crash semantics (Sentry's OnUnhandledRejection pinned to `mode:'strict'` for the same reason). JSON logs in production, pino-pretty in dev. Env (all optional): `SENTRY_DSN`, `SENTRY_ENVIRONMENT`, `SENTRY_RELEASE`, `LOG_LEVEL`, `LOG_PRETTY`. Tests: `tests/observability.test.js` (26 assertions incl. redaction + behavior-preservation; `npm test` in server/ runs both suites). **Scope decision:** client-side Sentry deferred out of T-002 (server-only instrumentation per approval) → folded into T-802 watch-page work. **Legacy quirk documented, deliberately preserved:** malformed JSON bodies answer `500 {"error":"Server error"}` (not 400) — fixed only by the `/api/v1` error contract (`08` §2), not here. Accept met: every request logs one line with request_id; unhandled errors reach Sentry when configured.

**T-003 · Baseline KPI capture** — ✅ DONE (2026-09-01, instrumentation; 7-day number collection pending production deploy). `server/kpi.js` (injectable-logger module) + 13 insertions in `server/index.js`: `upload_started`/`upload_finished` (outcome success|rejected_limit|error + code + sizeBytes + durationMs + store) on both upload handlers and the error middleware (multer-level failures imply attempts), `watch_404_retry`/`watch_recovered_after_404` (Cloudinary index-lag measurement keyed by recording id, 5-min window), 15-min `kpi_snapshot` aggregates (successRatePct excl. policy rejections, acceptRatePct incl., inFlightOrLost, p50/p95). Event schema, calculation rules, comparison method, and limitations recorded in `docs/BASELINE.md` §Baseline KPIs, with the 7-day pre-cutover table to fill from production logs before Phase 3. Tests: `tests/kpi.test.js` (29 assertions, unit + integration). Accept: instrumentation verified; **remaining acceptance item — fill the 7-day table from production before the Phase-3 cutover.**

## Phase 1 — PostgreSQL

**T-101 · DB scaffolding** — ✅ DONE (2026-09-01). `db/` package (`@veorec/db`): `env.js` (APP_ENV local|test|staging|production, defaults for local/test only, staging/production require explicit `DATABASE_URL`, destructive guard always refusing production, credential redaction), `pool.js` (pg Pool: timeouts, SSL, shared pool), `client.js` (Drizzle), `migration-state.js`, CLIs `db:migrate`/`db:status`/`db:reset`/`db:generate`; `migrations/0000_foundation.sql` (**no application tables** — `citext` + `set_updated_at()` only, so T-102 is a pure table diff) + journal; root `docker-compose.yml` (Postgres 16, Redis 7, MinIO, MailHog on non-default ports 5433/6380/9100/1025) and `.env.example`; `db/README.md`. Migration policy: forward-only, tracked, idempotent; `drizzle-kit push` never used; no down-migrations (rollback = corrective forward migration; `db:reset` is the local/test-only teardown) — rationale in `07` §14. Tests: `tests/db.test.js` (34 assertions: env guards, clean-DB migrate, idempotency, reset/re-migrate, citext/trigger existence, constraint conventions incl. FK cascade + CHECK + NOT NULL, teardown; skips loudly without infrastructure, `DB_TESTS_REQUIRED=1` enforces in CI). Accept met: `db:migrate` verified green against live PostgreSQL 16.15. **Deliberately NOT done:** no schema tables, no dual-write, no application code reads Postgres.

**T-102 · Schema migration 0001** — ✅ DONE (2026-09-01). All 30 tables from `07` §2–§10 as `migrations/0001_application_schema.sql` (46 FKs, 52 CHECKs, 58 indexes, `updated_at` triggers on 19 tables), with Drizzle definitions in `db/src/schema/` (identity, jobs, recordings, uploads, engagement, transcripts, editing, billing, misc). **Storage-provider-neutral**: media addressed only by `video_assets.storage_key`; no Cloudinary columns anywhere (asserted by test) — legacy identifiers get an isolated, migration-only `legacy_media_map` table in T-104, dropped in Phase 14 (`07` §13). Quota model represented: `usage` ledger (retained/reserved/pending-deletion/active-count/reserved-slots, all CHECK ≥ 0), `storage_reservations` (one open per upload session, upload-XOR-render), `upload_sessions.byte_ceiling`, `video_assets.counts_toward_quota`. Tests: `tests/schema.test.js` (46 assertions: schema parity vs Drizzle definitions, ER parity vs `07` §12, provider neutrality, UNIQUE/CHECK/FK/NOT NULL behaviour, cascade & set-null deletion, trigger). Accept met: clean-database migration verified; introspected FK graph matches the documented ER diagram. **Deliberately NOT done:** no repositories, no importer, no dual-write, no application reads/writes, no quota enforcement logic (T-306), no R2/Cloudinary work.

**T-103 · Repository layer** — ✅ DONE (2026-09-01). 18 repositories in `db/src/repositories/` (users, sessions, recordings, folders, assets, uploads, usage, shareLinks, comments, reactions, viewSessions, leads, analytics, transcripts, jobs, subscriptions, billingEvents, audit) behind `createRepositories(db)` / `repositories()` / `withTransaction(fn)`. **Scope-required**: owned-data methods take `{userId}` first and apply the ownership predicate in SQL (join-enforced for child rows); the unscoped surface is limited to explicitly named `*ForPublicWatch` / `*System` / `*AsAdmin`, the latter two requiring a stated reason. Field whitelists stop owners writing lifecycle/verified-media/ownership columns (invariant #11). **Transactions**: one executor bound per repo set; `usage.getForUpdate` refuses to run outside a transaction, giving T-306 atomic reservation without bypassing the layer. **Errors**: NotFound/Conflict/ConstraintViolation/InvalidState/Database with `retryable`, mapped from SQLSTATE through drizzle's wrapper — no raw pg errors escape, no HTTP concepts enter. Tests: `tests/repositories.test.js` (93 assertions incl. cross-user prevention on every owned entity, transaction commit/rollback, a two-transaction race proving exactly one reservation is granted, idempotent enqueue/webhook ledger, monotonic watch progress, timestamps). Docs: `07` §15. **Deliberately NOT done:** no application code uses it, no dual-write, no importer, no quota algorithm, no StorageProvider/R2, no Cloudinary changes.

**T-104 · Importer** — ✅ DONE (2026-09-01, code + fixtures; production dry-run still pending an operator with volume + credentials access). `db/src/migration/` (not `apps/api/…` — that app does not exist yet): `sources.js` (read-only readers, malformed JSON throws), `importer.js` (phased, FK-ordered, Drizzle-builder writes), `report.js` (per-entity source/imported/already/skipped/conflict/orphan/unmappedMedia/failed + problem list), `legacy-store.js` (transitional `legacy` schema), `export-legacy-media.js` (the **only** Cloudinary-touching file; resolves the SDK from the legacy server so `db/` declares no Cloudinary dependency), CLI `db:import` (**dry run by default**, `--apply` to write, exit 2 on problems, exit 3 if a legacy source changed). Migration `0002_legacy_migration_schema` adds `legacy.media_map` / `import_checkpoints` / `import_runs` in a **separate `legacy` schema** (invisible to drizzle-kit via `schemaFilter`, removed by one `DROP SCHEMA legacy CASCADE` in Phase 14). Idempotent via deterministic ids + ON CONFLICT, checkpoints for append-only logs; resume = re-run. No `video_assets` rows are created — media stays outside R2 until T-204. Tests: `tests/importer.test.js` (63 assertions covering the full required matrix). Docs: `07` §16. **Deliberately NOT done:** no runtime migration, no dual-write, no R2/StorageProvider, no Cloudinary removal, no legacy data deleted.

**T-105 · Dual-write** — ✅ DONE (2026-09-01, code; production rollout staged per `07` §17). `server/dualwrite.js` mirrors successful legacy mutations into PostgreSQL **behind `PG_DUAL_WRITE` (default OFF — only the literal `true` enables it; when off, no db code loads and no pool opens)**. Hooked at legacy store choke-points (`users.js`, `meta.js` folders + notifReads, `subscriptions.js`, `usage.service.js`, `contacts.js`) and at routes needing request context (`index.js`: both upload branches, meta/title patch, comment, reaction, view, lead). **Legacy always wins**: mirrors are queued after the response, errors are contained, and responses/JSON stores are byte-identical with the mirror failing (asserted). Single FIFO lane preserves causal order (concurrent mirrors raced FKs — real defect found in testing), bounded queue sheds to the journal, circuit breaker prevents backlog during an outage. Failures append to `dual-write-failures.jsonl` (durable even when Postgres is down) with op/entity/legacyId/requestId/errorCode/constraint/retryable; `db:reconcile` verifies + prunes, repair reuses the idempotent T-104 importer. Writes share `mirrors.js` with the importer so mirrored and imported rows are identical. KPI: dualWrite attempts/success/failures/retryable/reconciliationPending. Tests: `tests/dualwrite.test.js` (57 assertions, real servers + real PostgreSQL). **Deliberately NOT done:** no PostgreSQL reads, no read cutover, no quota enforcement, no R2/StorageProvider, no recorder/client/extension changes, recording deletes not mirrored (see limitations).

**T-106 · Reconciliation safety net** — ✅ DONE (2026-09-01, commit `d389ede`). `db/src/migration/reconciler.js` + report-by-default CLI `db:reconcile`. Eight detection classes: `legacy_missing_in_pg`, `field_drift` (critical for subscription entitlements), `stale_in_pg` (**unmirrored deletes**), `failed_dual_write`, `orphaned_child`, `mapping_inconsistency`, `unsafe`, `repair_failed`. **`--report` is the default and performs zero database writes**; `--repair` is explicit, additive and idempotent, reusing the shared `mirrors.js` mappers. **No hard-delete path exists** — `--allow-stale-soft-delete` only sets `deleted_at` (reversible), is refused outside `--repair`, and never runs unattended; ownership disagreements are never auto-rewritten. Delete detection is **skipped with a loud note** when no media listing is supplied rather than guessed from incomplete input, so live production recordings can never be falsely reported stale. Exit codes 0 convergent / 2 findings / 1 error for cron alerting. Tests: `tests/reconciler.test.js` (45 assertions, real PostgreSQL, every detection class). Docs: `07` §18. **Deliberately NOT done:** no PostgreSQL reads, no read cutover, no quota enforcement, no R2/StorageProvider, no Cloudinary changes, no legacy data deleted. Remaining gate: report must be clean on consecutive nightly runs before any read cutover. Original objective — not merely a nightly report: the mechanism that *proves* PostgreSQL stays convergent with the legacy system throughout the migration, and the gate that must be clean before authority ever moves. Detects, at minimum: legacy records missing in PostgreSQL · PostgreSQL records missing in legacy (**including unmirrored deletes**) · field drift on important columns · outstanding failed dual-writes · orphaned child records · mapping inconsistencies (bad id derivation, ownership mismatch, duplicates) · anything that cannot be safely reconciled automatically. **Detection first: `--report` is the default and performs zero writes; repair requires an explicit `--repair`, and no nightly run may ever delete PostgreSQL data.** Deps: T-105. Accept: every detection class exercised by tests; nightly report runs read-only; diffs trending to zero before any read cutover.

## Phase 2 — Storage

**T-201 · StorageProvider** — ✅ DONE (2026-09-01; **contract suite VERIFIED against MinIO AND real Cloudflare R2 staging** — 59/59 on both). `storage/` package (`@veorec/storage`, not `apps/api/src/storage/` — that app does not exist yet, same deviation as T-104): `config.js` (env-only, no hardcoded ids/keys/buckets/endpoints; staging/production have **no defaults** and require https; credentials absent from `describe()`/`toJSON`/`util.inspect`), `keys.js` (canonical `02` §2.7 layout, strict-charset id validation *before* interpolation, client filenames never trusted, deterministic keys), `errors.js` (10-code taxonomy with `retryable`; explicit S3 code beats HTTP status; no raw SDK error or HTTP status escapes), `s3-provider.js` (**the only file importing `@aws-sdk/*`** — asserted by test), `index.js` (public surface). **One implementation for R2 and MinIO** — `STORAGE_PROVIDER` selects a configuration flavour and no code path branches on it (asserted). Signed URLs are per-object, per-method, TTL-capped (over-ceiling requests are **refused, not silently shortened**); `contentLength` is a **signed header**, giving T-306 a provider-enforced byte ceiling rather than a client-side promise. Multipart create→presign part→complete|abort, with abort/delete safely repeatable and completion keyed by `(key, uploadId)` so a retry cannot duplicate an object. Tests: `tests/storage.test.js` (**110 assertions, no infrastructure** — keys/traversal, config + secret hygiene, error mapping, and real SigV4 presigning: method binding, object scoping, expiry capping, Content-Length signing) + `tests/storage-contract.test.js` (**abstract contract suite** — imports no SDK, names no bucket, so MinIO and R2 run identical assertions; skips loudly without an endpoint, `STORAGE_TESTS_REQUIRED=1` enforces in CI, refuses `APP_ENV=production`). Docs: `02` §2.7.1, `storage/README.md`. **Acceptance — MinIO: MET.** The contract suite was executed against a real S3-compatible store (MinIO RELEASE.2025-09-07T16-13-09Z, temporary local process, `http://127.0.0.1:9100`, bucket `veorec-media-test`): **59 assertions passed, 0 failed** covering put/get/getBuffer/head/exists/delete/deleteMany/list, signed GET+PUT (method binding, object scoping, expiry, tampered-signature and unsigned-request rejection, Content-Length enforcement), the full multipart lifecycle (create/part URLs/listParts/complete/abort, incomplete upload not exposed as an object), isolation and real error mapping. **Two defects were found by this run and fixed** (see `02` §2.7.1): the signed-header set was a fixed superset, so a byte-ceiling-only upload URL also signed `content-type` and any client omitting that header got HTTP 400 — the enforceable ceiling was in fact broken until integration testing exposed it; and the contract suite pinned a provider-specific abort response shape that MinIO and AWS legitimately differ on. **Acceptance — R2: MET (2026-09-04).** The same contract suite ran against a real Cloudflare R2 staging bucket: **59 passed, 0 failed**, covering unsigned-GET refusal, presigned GET/PUT with method binding and object scoping, signed Content-Length enforcement (exact accepted, oversize and undersize refused), the full multipart lifecycle, isolation and error mapping. **Two defects surfaced only on R2 and were fixed:** (1) R2 reports authorization failures as `400 InvalidArgument("Authorization")` where S3/MinIO use `403`, so wrong or rotated production credentials were classified `invalid_request` instead of `permission_denied` — the mapper now reclassifies authorization-shaped `InvalidArgument`, narrowly, so a genuine bad argument still maps to `invalid_request`; (2) the contract suite pinned the provider-specific status set `{401,403,404}` for an unsigned request and now asserts refusal (`>=400`), and it uses `getObject` rather than `headObject` for the bad-credentials case because a HEAD carries no response body and R2 error codes are therefore invisible on it. Both re-verified green on MinIO. **Deliberately NOT done:** no bucket/lifecycle/CORS provisioning (T-202), no upload mirroring (T-203), no backfill (T-204), no upload-session API (T-301), no quota reservation (T-306), no application code calls the package, no Cloudinary change, no legacy behaviour change.

**T-202 · Buckets & lifecycle** — ✅ DONE (2026-09-04, configuration + tooling; **CORS and the 48h abort rule PENDING R2 staging verification**). `storage/src/bucket-config.js` (provider-neutral CORS + lifecycle declarations), `storage/src/provisioner.js` (apply/verify), CLI `storage:provision` (**`--check` read-only by default**, `--apply` explicit; never creates a bucket, never deletes an object). Control plane kept OUT of `StorageProvider`: bucket administration has no application-level meaning and a `putBucketCors()` on the provider would let any caller reconfigure the bucket, so the SDK lives in exactly two files (`s3-provider.js` data plane, `provisioner.js` control plane) and provisioning is not exported from the public surface — asserted. **Lifecycle:** bucket-wide `AbortIncompleteMultipartUpload` at 48h (safe bucket-wide — it cannot touch completed objects, and scoping it to `uploads-tmp/` would miss sources written via multipart per `06` §11) + object **expiration confined to `uploads-tmp/`**; `assertNoDurableExpiry()` refuses any rule, generated or already on the bucket, that would expire `sources/`/`derived/`/`audio/`/`renders/` or expire bucket-wide. Lifecycle is a backstop only — the hourly expiry job (`06` §8) remains authoritative because it also releases the quota reservation. **CORS:** explicit origins from `STORAGE_CORS_ORIGINS` (**wildcards refused**; no production domain invented as a default; deployed origins must be https/chrome-extension), methods `GET`/`HEAD`/`PUT` only, `ETag` exposed (without it every browser-direct multipart upload fails at completion), `Content-Length` deliberately not listed since the ceiling is enforced by the signature. Tests: `tests/storage-buckets.test.js` (67 assertions). Docs: `02` §2.7.2, `.env.example`. **Acceptance — locally VERIFIED:** private-bucket check, unauthorized GET refused, `uploads-tmp/` expiry rule accepted and read back by a real S3 backend, durable-prefix guard. **Acceptance — FULLY VERIFIED against real Cloudflare R2 staging (2026-09-04, bucket `veorec-staging-2026`).** Bucket private (proven empirically: unauthorized GET refused — R2 does not implement `GetBucketPolicyStatus`, so the policy-based check reports `unsupported` there and the unsigned-GET refusal is the stronger proof). Lifecycle applied and read back semantically: **incomplete-multipart abort = 2 days (48h)** — replacing R2's 7-day default — and **`uploads-tmp/` expiration = 2 days**, with **zero** durable prefixes subject to expiry. CORS applied and read back: origins `["http://localhost:5173"]`, methods `GET/HEAD/PUT`, allowed headers `content-type`/`x-amz-checksum-crc32c`, **exposed `ETag`**, max-age 3600. **Browser CORS verified with an actual browser**, not an SDK request: a real cross-origin `PUT` from `http://localhost:5173` preflighted and succeeded (HTTP 200) **and the response `ETag` was readable from script**, which is what makes browser-direct multipart completion possible; the same page served from `http://localhost:5174` was **blocked at the preflight** by Chrome ("No 'Access-Control-Allow-Origin' header is present"), so a disallowed origin receives no permission. Contract suite **59/59 against R2** and **59/59 against MinIO**. **CORS policy change (approved):** explicitly configured **loopback** http origins are permitted in local/test/staging — browsers treat `http://localhost` as a secure context because the traffic never leaves the machine — while **production refuses every plaintext origin, loopback included**, and non-loopback http is refused in every environment. Regression coverage added for all four cases (loopback allowed in local/staging, arbitrary http refused, production http refused, wildcards refused). Staging bucket left with the intended configuration and 0 objects / 0 incomplete multipart uploads. **Not claimed:** production provisioning — the production bucket is untouched. **Deliberately NOT done:** no upload mirroring (T-203), no backfill (T-204), no upload-session API (T-301), no quota reservation (T-306), no application code calls the package, no Cloudinary change, no production provisioning.

**T-203 · Mirror new uploads to R2** — ✅ DONE (2026-09-10, code + integration verification; **the 1-week production acceptance window still has to be observed**). `server/r2mirror.js` mirrors the bytes of a *successful* legacy upload to `sources/rec_<legacyId>/source.<ext>` through `StorageProvider`, then records a `video_assets` row. **Behind `R2_MIRROR_UPLOADS` (default OFF — only the literal `true` enables it; when off the storage package is never loaded and the handler behaves exactly as before, so disabling is an instant rollback).** **Legacy stays authoritative:** the mirror runs after the response is sent, never throws into the request, and is asserted to leave an upload succeeding even with object storage unreachable. **Ordering:** the asset row is written on T-105's single FIFO lane via `dualwrite.recordingAsset()`, because `video_assets` has an FK to `recordings` and any other path would race its parent — the defect T-105 already hit. **Bytes first, row second:** a failed upload writes no row, so a row never claims a nonexistent object; the reverse leaves an orphan object, which is detectable and the safe direction. **Verified before the row is written:** the object is `HEAD`ed and its size compared to the uploaded byte count, so a truncated copy is never recorded as a good source. **Temp-file ownership** moves to the mirror in the Cloudinary branch (so the bytes survive long enough to stream) and it removes the file on every path including failure; in the local-disk branch `deleteAfter:false` — that file is the permanent legacy store, not a temp file. **Idempotent:** deterministic storage key + `assets.upsertSourceSystem()` (new repository method, `ON CONFLICT (storage_key)`) so a replayed mirror converges on the same row instead of violating the unique index, and an upsert can never re-point an object at a different recording. Bounded concurrency + queue shedding + durable journal `r2-mirror-failures.jsonl` (identifiers only, no credentials — asserted). KPI: `r2MirrorAttempt/Success/Failure` plus a `mirroredRatePct` that is exactly the acceptance number. Tests: `tests/r2mirror.test.js` (**43 assertions**: disabled-by-default and flag parsing, temp-file ownership both ways, real server + real MinIO + real PostgreSQL for object/size/bytes/row correctness, cross-user isolation, idempotency, invalid re-point, provider failure leaving the upload successful, journal hygiene, mirror-on/dual-write-off, cleanup). **Also verified end-to-end against real Cloudflare R2 staging**: a live upload produced the object at the expected key with matching size and byte-identical content, then was cleaned up. Docs: `02` §2.7.3, `.env.example`. **Deviation from the canonical wording, deliberate:** the canonical text says "after Cloudinary success" and both upload branches are hooked instead — the acceptance criterion is "100% of new uploads mirrored", which a Cloudinary-only hook would leave false on any non-Cloudinary deployment and untestable end-to-end. **Acceptance NOT yet met:** "100% of new uploads mirrored for 1 week" is a production observation window; enable the flag in production and watch `kpi_snapshot.r2Mirror.mirroredRatePct`. **Deliberately NOT done:** no backfill of existing recordings (T-204), no upload-session API (T-301), no quota enforcement (T-306), no Cloudinary removal or expansion, no read switched to PostgreSQL or R2, no recorder/IndexedDB/watch-page/billing change.

**T-204 · Backfill copier** — ✅ DONE (2026-09-10, code + integration verification; **the production backfill run itself is an operational step still to be performed**). `db/src/migration/backfill.js` + CLI `db:backfill` (**dry run by default**, `--apply` explicit, `--limit`/`--concurrency`/`--batch`). Copies each existing legacy original into R2 at `sources/{recordingId}/source.{ext}` via `StorageProvider` and records the `video_assets` row with `assets.upsertSourceSystem` (reused from T-203). **COPYING IS NOT CUTTING OVER:** a verified object is an ADDITIONAL copy — Cloudinary keeps serving every read, and the copier contains **no delete call of any kind** against either provider (asserted over the stripped source). No application read changed. **Checklist:** migration `0003_backfill_checklist.sql` extends `legacy.media_map` (the row T-104 created for this, per `07` §13) with `backfill_status` (`pending`/`claimed`/`verified`/`failed`/`skipped`/`unsafe`), `backfill_attempts`, `backfill_claimed_at`, `backfill_claim_id`, `backfill_retryable`, `backfill_bytes`, plus a CHECK making `verified` and `backfilled_at IS NOT NULL` equivalent so "the database says done" cannot drift from "the object was checked". **Claiming:** one atomic `UPDATE … FOR UPDATE SKIP LOCKED` — two workers never take the same row (asserted with concurrent claims), the claim commits **before** any byte moves so no transaction is held across a multi-minute transfer, and a crashed worker's claim **ages out** and becomes re-claimable. **Order:** claim → transfer (no txn open) → **verify by re-reading the object and comparing size** → asset row → checklist stamp. A crash between transfer and stamp converges on the next run because the key is deterministic and the row write is an upsert; the reverse order could claim an object that never landed. **Retry policy:** each item gets **one attempt per run** — an earlier draft re-claimed a transient failure inside the same loop, burning its whole attempt budget at once and hammering a provider already returning 5xx (found by testing, fixed). 5xx/429 retryable; 404/410, size mismatch, unsupported container and ambiguous mapping are **not**, and an attempt ceiling stops infinite retries. **Ownership is never guessed:** ambiguity → `unsafe` for human review; an existing object whose size disagrees with the legacy record is never overwritten; the upsert cannot re-point an object at another recording (asserted). **T-203 overlap:** a recording already mirrored is recognised via `headObject` and marked verified **without re-reading the legacy source** (asserted: zero legacy bandwidth). Tests: `tests/backfill.test.js` (**61 assertions**, real PostgreSQL + real MinIO) covering all 23 required scenarios. Also verified against **real Cloudflare R2 staging with synthetic fixtures** (object created, size matched, bytes identical, checklist stamped, cleaned up; staging left at 0 objects / 0 multiparts with T-202 config intact). Docs: `07` §19, `02` §2.7.4. **Acceptance NOT yet met:** "100% of non-deleted recordings have R2 sources" requires running the backfill against production data — the tooling is ready, the run is an operator action. **Deliberately NOT done:** no read cutover, no Cloudinary deletion or expansion, no upload-session API (T-301), no quota enforcement, no legacy JSON removal, no BullMQ/FFmpeg, no watch-page change.

## Phase 3 — Upload system

**T-301 · Upload session endpoints** — ✅ DONE (2026-09-10). New `api/` package (`@veorec/api`) with the six endpoints from `08` §5, mounted on the legacy server behind **`V1_UPLOAD_API` (default OFF** — when off none of the new packages load and the legacy app is byte-identical, so disabling is a complete rollback). The router receives its dependencies and knows nothing about its host, so relocating it to a standalone `apps/api` is later a deployment change rather than a rewrite. **Layering asserted by test:** no SQL, no storage SDK and no Cloudinary reference in the API layer — PostgreSQL only through T-103 repositories, object storage only through T-201 `StorageProvider`. **Ownership:** every lookup is a scoped repository call, and another user's session returns **404 not 403** so an id is never confirmed to an attacker; cross-user access is refused on all five session routes. **Byte ceiling enforced twice:** the server refuses to *mint* URLs beyond it, and each URL is signed with its exact `Content-Length` so storage rejects a larger body. **Completion:** `CompleteMultipartUpload`+`HEAD` run OUTSIDE the transaction — holding one across a network call would pin a connection — which is safe because `06` §7 step 3 makes a crash there recoverable (upload gone + object present ⇒ success); **that exact crash is injected and verified**. One short transaction then commits session `completed`, recording `uploaded` with the **real** stored size, the `video_assets` source row and the `processing_jobs` **outbox** row together; an injected failure at the last step is verified to roll back all four. **Idempotency:** repeated completion replays the canonical result with no second asset and no second probe job; two *simultaneous* completions produce exactly one of each; part recording upserts; session creation is keyed by `Idempotency-Key` and a second session for a recording returns the live one. **Resume:** `GET` merges our rows with live `ListParts` and **storage wins**. **Errors:** the nested `08` §2 contract; no raw PostgreSQL or storage internals escape. Tests: `tests/uploads-api.test.js` (**108 assertions**, real PostgreSQL + real MinIO + a spawned-legacy-server mount check). Docs: `02` §2.7.4. **Scope boundary (documented, not silent):** `06` §3 and `08` §5 put an atomic quota reservation in session creation, while this plan puts the ledger in **T-306** — T-301 therefore takes the endpoints and leaves the ledger, so `byteCeiling` comes from the plan rather than reserved quota and entitlement-at-complete is a pluggable check T-306 replaces. **Deliberately NOT done:** no quota ledger/reservation (T-306), no client uploader, no expiry job, no probe relay or workers (T-601), no legacy route change, no read cutover, no recorder/IndexedDB/watch-page/billing change.

**T-302 · Recordings CRUD v1** — ✅ DONE (2026-09-11). `api/src/recordings.router.js`: `POST` / `GET` (list) / `GET /:id` / `PATCH /:id` / `PATCH /:id/meta` / `DELETE /:id`, all from PostgreSQL through T-103 repositories. Mounted on the same `V1_UPLOAD_API` flag as T-301 (**default OFF**); the legacy `/api/recordings` routes are untouched. **List from DB:** one indexed, scoped query with cursor pagination and folder/archived filters — the Cloudinary dual-fetch the dashboard needs today is gone. **Authz matrix (canonical test):** every route exercised owner / non-owner / unauthenticated; a recording belonging to another user is **404, indistinguishable from missing**, so an id is never confirmed; a non-owner `DELETE` is verified not to delete and a non-owner `PATCH` not to modify. **Soft delete + usage tx (canonical test):** one transaction locks the usage row with `getForUpdate` (which refuses to run outside a transaction), **re-reads the recording inside the lock**, soft-deletes, and moves bytes retained → pending-deletion while decrementing the video count and seconds — quota freed immediately, bytes awaiting the 30-day purge (`16` Q15). Three simultaneous deletes decrement **exactly once**; a repeated delete does not decrement again; deltas are **clamped** so an un-incremented ledger cannot be driven negative into its CHECK constraints; an injected ledger failure **rolls the soft delete back with it**. This is ledger maintenance on delete — the atomic reservation that gates uploads remains **T-306**. **Create idempotency without a new column:** the id is derived from `sha256(userId:Idempotency-Key)`, so a replay collides on the primary key and returns the same row, and two users' identical keys cannot collide. **Pro gates are paywalls:** `password`/`removeBranding` answer `403 feature_locked` + `upgradeRequired` rather than being silently dropped; passwords are hashed before storage and never echoed. **No storage key leaves the API** — detail returns a short-lived signed URL minted only after the scoped read. Tests: `tests/recordings-api.test.js` (**93 assertions**, real PostgreSQL). Docs: `02` §2.7.5. Accept met: the dashboard *can* render from v1 behind the flag (nothing points at it yet — that is T-803). **Deliberately NOT done:** `POST /:id/duplicate` (needs the T-601 copy-job pipeline) and `POST /:id/thumbnail` (Pro image upload) — neither is CRUD; no quota ledger/reservation (T-306), no legacy route change, no client change, no read cutover.

**T-303 · Extension uploader module** — ✅ DONE (2026-09-11). `extension/uploader.js` (**not `.ts`** — the extension is plain MV3 JavaScript with no TypeScript toolchain or build step; introducing one would change how every other extension file ships). Part buffering sealing at **≥** `partSize` (8 MiB) so a non-final part can never fall under S3's 5 MiB floor, prefetch presign (batches of 5, 3 ahead), concurrency **2 while recording / 4 after stop**, the full retry/backoff classifier, and the resume diff. **Wired alongside the legacy path, never instead of it:** `ondataavailable` still fills the legacy `chunks` array (so save-to-device and the legacy POST remain available) and additionally feeds the uploader, wrapped so an uploader fault cannot interrupt the recording; at Stop the legacy POST is skipped only if the streaming upload finished, otherwise the recorder falls through to it with the blob it still holds — **a take is never lost to the new path**. Behind `newUpload` in `chrome.storage.local`, **default OFF** (only the literal `true` enables it) — **superseded by T-304**, which removed that client-side flag so the SERVER alone decides the path. **Retry classifier:** network/timeout/5xx/429 → retry with exponential full jitter (8 attempts); **403 with an expired signature → re-mint the URL, NOT a data attempt**; any other 4xx → fatal; exhaustion → `stalled` (session and bytes survive, whole drain retried every 60s). A fatal verdict is never downgraded to `stalled`. **Byte ceiling:** warn at 90%, stop at `ceiling − 16 MiB` so the final part still fits; a ceiling refusal at presign is fatal. **Resume:** the server view wins, so an already-uploaded part is adopted rather than re-sent. CRC32C per part (verified against the standard vector). **Two real defects found by the tests and fixed:** `drain()` returned early while another drain was in flight, so `finalize()` could run before the parts finished — in production Stop would have reported "outstanding" and fallen back to the legacy upload for no reason; and a fatal verdict was being masked as `stalled`, which would have made the UI retry a plan rejection forever. Tests: `tests/uploader.test.js` (**79 assertions, mock fetch**, every retry branch, backoff bounds and jitter, buffering, concurrency ceiling measured, resume diff, byte ceiling, completion verdicts, and the wiring assertions) — **R14 and R15 both green with the flag on**. Additionally a **real-browser happy path**: the real `uploader.js` served from `http://localhost:5173` ran a full session against the real `/api/v1` API and real MinIO with `Blob` chunks — 5,243,904 bytes uploaded, object present at the right size with correct byte ordering, recording `uploaded`, one asset row, one probe job. Docs: `04` §5.1. **Playwright specifically was NOT used** — it is neither installed nor declared, and adding it pulls ~300 MB of browsers; the browser path above covers the same ground with the in-app browser. **Deliberately NOT done:** no IndexedDB durability (Phase 4), no recorder state-machine rewrite, no web upload path (T-305), no cutover flag plumbing or telemetry (T-304), no legacy route change, no client ships the flag enabled.

**T-304 prerequisite · Legacy→PostgreSQL identity bridge** — ✅ DONE (2026-09-11). Discovered during T-303 and accepted as a **blocking cutover issue**: the legacy JWT carries the legacy user id while every mirrored PostgreSQL row is keyed `usr_<legacyId>`, so v1 ownership scopes matched nothing and a create failed on the `users` FK. `api/src/identity.js` translates **once**, at the v1 router boundary, using the **canonical `idFor` from `db/src/legacy-ids.js`** (a test asserts the bridge holds that exact function, not a copy). `req.userId` is deliberately untouched — the same `requireAuth` instance serves **59 legacy routes** that read it as the legacy id — and the translated value lands on `req.pgUserId`. A missing mirror answers **`503 account_not_migrated`** rather than surfacing as a foreign-key `422`, because the caller is legitimately authenticated and the condition is a temporary server-side migration state (and the count is exactly what an operator needs during the staged rollout). `scopeOf(req)` is the only way a v1 scope is built and **throws** on an untranslated request rather than falling back to the legacy id. Tests: `tests/identity-bridge.test.js` (**39 assertions, real PostgreSQL**) — the canonical mapping is used, a legacy-id caller reaches the imported identity, **no user is created at runtime**, all T-301 and T-302 endpoints work for that caller with no hand-seeded raw-id user, ownership isolation survives, an unmirrored account fails predictably and discloses nothing, and the legacy identity is preserved. **Deliberately NOT done:** no runtime user creation or seeding, no additional dual-write record, no change to PostgreSQL user ids, and no change to the importer, reconciler or dual-write mapping. A **migration-window shim**: it disappears when T-1302 makes PostgreSQL the authentication source of truth. Docs: `02` §2.7.6.

**T-304 · Cutover flag & telemetry** — ✅ DONE (2026-09-11) — **code and local verification complete; the production two-week observation remains outstanding.** Approved at commit `44b2ba6`; the two-week production acceptance criterion is explicitly NOT approved as complete. `server/rollout.js` + `GET /api/client-config` + path-tagged KPI telemetry. **The server decides, the client obeys** — a client cannot opt itself into the rollout, which is why T-303's `newUpload` flag in `chrome.storage.local` is **removed** rather than kept as a second gate (an AND of the two would have made a staged rollout impossible; an OR would have made it unenforceable). **Bucketing:** `sha256("veorec-upload-cutover-v1:" + userId)` → first 4 bytes as uint32 → `% 100`, selected iff `bucket < percent`. Deterministic (no randomness, no clock, no per-process counter — all three would flip a user between requests and lose an in-flight session) and **monotonic**: 10% ⊂ 50% ⊂ 100% is asserted exactly, and a test walks **all 100 one-point steps** proving a raise never removes a previously selected user. **Fail-safe:** absent/malformed/out-of-range config ⇒ 0%; `V1_UPLOAD_API` must be exactly `true`; any failure while deciding resolves to `legacy`, never `v1`. **A real defect found by the tests:** `Number('1e2')` is `100`, so `V1_UPLOAD_ROLLOUT_PERCENT=1e2` silently meant a **full rollout** — exactly the "a config typo switches everyone on" case the module claims to prevent; the parser now requires a plain decimal integer (`' 100 '`, `'0x64'`, `'100.0'` and `true` all resolve to 0). **`account_not_migrated` is its own decision and its own counter**, never folded into a generic legacy result: a bucket-selected user whose PostgreSQL mirror is missing still gets a working upload on the legacy path, and the population stays visible instead of hiding behind a healthy-looking success rate (the mirror is checked **only** for bucket-selected users, so an unselected user costs no query). **Telemetry:** `upload_path` and `fallback_from` on `upload_started`/`upload_finished`, a `rollout_decision` line per lookup, and a `cutover` block in the 15-minute snapshot. A take that starts on v1 and is rescued onto legacy counts in **both** `uploadV1Fallback` and `uploadV1Failure` — the fallback exists so users never see a failure, so a naive rate would read 100% *precisely because* the failures were hidden. No user id, token, cookie or credential appears in any decision or upload line (asserted). **Rollback drilled deliberately at integration level**, not by unit-testing a helper: a real spawned server at 100% selected a real signed-up user for `v1`; the percentage was set to `0` and the **same** user resolved to `legacy` with `decision:"legacy_disabled"` logged; `users` count 7 → 7, no rows or files changed. Config-only, no code change, no client release, no migration. Tests: `tests/rollout.test.js` (**78 assertions**, real PostgreSQL + two real spawned servers). Docs: `08` §14a (response contract), `19` §8 (fields, decisions, success-rate formula and denominator, what "verified" means), `23` Phase 3 cutover (algorithm, 0→10→50→100 procedure with per-step gates, rollback procedure). **Deliberately NOT done:** the acceptance criterion is **NOT met** — ≥99% over two weeks requires production traffic and no production window exists; local and staging verification does not and cannot satisfy it. No quota reservation (T-306), no web upload path (T-305), no change to `server/auth.js` or legacy authentication, no `req.userId` reassignment, no runtime user creation, no legacy path removal, and no client ships with the rollout above 0%.

**T-305 · Web upload path** — ✅ DONE (2026-09-11), approved at commit `646e5e4`, for what it names; **composition of a v1 clip is explicitly deferred to Phase 7/12 and the production web gate stays OFF until then.** **Single-PUT mode** added to the T-301 router (`mode:'single'`, `06` §12): `sizeBytes` required and signed into the PUT URL as the exact `Content-Length` so storage itself refuses any other size (verified against MinIO: a one-byte-short PUT is refused, the exact one succeeds); ceiling `min(plan, 33,554,432)`, a larger declared size is a `403 storage_limit` paywall at create; no multipart is opened (`storage_upload_id` NULL, `mode='single'` — both already in the schema, **no migration**); `uploadUrl`/`uploadHeaders` in the create body, re-minted on replay; part endpoints `409 invalid_state`; complete with `parts:[]` HEADs the object (`409 upload_object_missing` before the PUT, session still usable) and runs the **same transaction** as multipart — session, recording, ready source asset, probe outbox row; abort of an incomplete single session deletes its orphaned object; multipart untouched (asserted). **Dedicated web gate `V1_WEB_UPLOAD`**: exact `'true'` only, and only while `V1_UPLOAD_API` is `true` — neither flag implies the other; `'TRUE'`, `'1'`, `'yes'`, whitespace and every malformed value → legacy; surfaced as a separate `webUpload` block on the existing `/api/client-config` (no parallel config system); **independence proven both ways** — enabling it changes zero extension decisions across a 2,000-user population at 0/10/50/100%, a 100% extension rollout does not enable it, and its code references no bucket, percentage or salt. Same mirror rule as the extension (`account_not_migrated` on its own `web_upload_decision` line). **Editor "Add video → Upload"** (`client/src/lib/v1Upload.js`, `Editor.jsx`): server-decided; browser-direct PUT to the presigned URL; no bytes to `/api/*`, no FormData/multer, no storage SDK or credential in the browser (asserted). **Fallback boundary is exactly session creation**: a failed config fetch, pre-flight, recording create or session create (the recording row is deleted first — no empty duplicate) may fall back to the legacy POST; after a session exists there is **no** fallback — the error is shown, because a second upload would duplicate the file and the recording. **Composition protection**: `/compose` answers `409 clip_not_composable` (never a generic 404) for a v1 clip, checked before the backend/plan checks so the answer is the same on every backend; nothing copied into Cloudinary, nothing rendered; the editor explains it in plain words before sending (0 compose requests on Save) and also handles the server's own 409. **Memory-multer `POST /api/recordings/:id/replace` deprecated, NOT removed**: functional and unchanged, code-level marker, `warn`-level `deprecated_replace_used` event per use, `kpi.legacyReplaceUsed` counter surfaced as `deprecations.legacyReplaceUsed` — the Phase 14 removal gate; it has **no caller** in the client or extension today, so the counter is what turns that into evidence. **A real defect found by the browser run and fixed:** MediaRecorder WebM reports `duration = Infinity`, so the uploaded clip measured 0 s and was a zero-width, invisible timeline entry; `measureDuration` now forces the scan (seek-to-end, `durationchange`) — the same clip then shows 0:12. Tests: `tests/uploads-api.test.js` +38 (**146**, real PG + MinIO, single mode end-to-end incl. storage-enforced Content-Length, byte-identical retrieval via a signed URL, abort cleanup, plan ceiling, multipart non-regression) and `tests/web-upload.test.js` (**90**: gate defaults and every malformed value, two-way independence, telemetry counters and log shapes, source-level client wiring and fallback boundary, and a real spawned server — `/client-config` web decision, `409 clip_not_composable`, legacy clips untouched, replace route working **and** counted exactly once, config-only rollback, wrong-case gate → legacy). **Browser E2E on the real Vite client against a real server, PostgreSQL and MinIO:** gate ON → `client-config` → `POST /api/v1/recordings` → `POST /api/v1/uploads` → **browser PUT straight to 127.0.0.1:9100** (`x-id=PutObject`, signed `content-length;content-type;host`, 200) → `complete` → `GET /api/v1/recordings/:id`; **zero** requests to `/api/upload`; recording `uploaded` at exactly 2,868,138 bytes; a `<video>` decoded the signed `playbackUrl` (1920×1080, 12.206 s); clip in the timeline at 0:12; Save showed the not-composable message and sent 0 requests; `/compose` from the page → 409. Gate OFF (config-only restart, same account) → 1 legacy `POST /api/upload`, **0** `/api/v1` calls, 0 storage PUTs — legacy behaviour unchanged. 0 Cloudinary writes. Full regression **1,307 assertions, 0 failures, 0 skips**. Docs: `06` §12 reconciled (deprecated in T-305 → removed in Phase 14, no longer "deleted"), `08` §5 + §14a, `19` §8.3, `23` Phase 3 web path. **Deliberately NOT done:** no composition/save of a v1 clip (Phase 7/12), no dashboard (`AppShell`) upload switch (the canonical acceptance names the editor; the library is shared and ready), no quota reservation (T-306), no removal of the replace route (Phase 14), no read cutover (T-803), no change to T-304 bucketing or `server/auth.js`, no schema change. **Not verifiable here:** legacy Cloudinary composition end-to-end (no Cloudinary locally) — verified only that legacy clips pass the new protective check untouched and reach the backend's own answer.

**T-306 · Quota ledger & atomic reservation** — ✅ DONE (2026-09-11), autonomous (user waived per-task approval; my recommended resolutions applied). **No schema change** — every ledger column, `storage_reservations` and `counts_toward_quota` already existed. **Atomic check-and-reserve** (`usage.repo.reserveGuarded`, `api/src/quota.js`): one guarded UPDATE on the `usage` row, taken after `SELECT … FOR UPDATE`, inside the same transaction that inserts the session and the `storage_reservations` row; `reserve = min(max_upload_bytes, available)`, floor `min_start_bytes` (a single-PUT file's floor is its declared size); guard failure ⇒ `403 storage_limit|video_limit` with the `16` §4.6 messages and real numbers in `meta`, **no session created**. **Resolution of the dual-write conflict (Option A):** the guard's retained-bytes and active-count terms are sub-selects over `recordings`/`video_assets` inside that same statement, never the counters that the legacy mirror and the reconciler overwrite; `/me/usage` reads the same aggregates so meters and gate cannot disagree; the counters remain caches healed by `usage_sync` (`16` §4.7). **Reconciliation** at completion under the row lock with the HEAD size (retained += real, reserved −= reservation, slot back, active += 1, reservation `reconciled`); **release** on abort, over-ceiling manifest, expiry, and a completion the plan re-check refuses (`rejected_limit`, source kept 7 days); sessions predating the ledger complete and abort safely with nothing released below zero. **Three-layer ceiling** wired: `byteCeiling = reservation` → recorder auto-stop (T-303), presign refusal + signed Content-Length (T-301), exact completion check (T-301). **Dual meters** `GET /api/v1/me/usage` (`16` §4.5 shape, no blended percentage). **Plan catalog:** the `16` §1.1 integers added to `server/plans.js` as `QUOTA_V2`; `plans.limitsFor()` returns the legacy fields unless `QUOTA_ENFORCEMENT_V2` is exactly `true` (byte-identical to today when OFF; the notice-period flip for both the v1 gate and legacy `canUploadVideo`/`canCreateVideo` when ON); **grandfathering is derived, not stored** — an over-cap user is blocked from new recordings by the ordinary guard, nothing trimmed. **Jobs:** `maintenance.usage_sync` (daily; re-derives counters, expires orphaned reservations, logs drift ≥1%) and `maintenance.upload_expiry` (hourly; expires sessions, tears down storage, releases reservations) as `@veorec/db` functions + CLI, scheduled by the legacy in-process cron until Phase 6. **Two real defects found by the tests and fixed:** (1) two untyped SQL parameters compared to each other resolve as *text* — `'536870912' >= '67108864'` is false — so the guard silently refused every real-sized reservation until every parameter was cast; (2) an over-ceiling manifest aborted the session but never released its reservation, which would have sat held until expiry. Tests: `tests/quota.test.js` (**83 assertions**, real PostgreSQL + MinIO): Q1 (two tabs, and **50 parallel** creations against one slot — exactly one wins, ledger never over-reserved, no session for the losers), Q2, Q3, Q4 (replay reserves once), Q5 (double complete counts once), Q6 (expiry releases; re-run releases nothing), Q7 (re-abort idempotent), Q8 (HEAD size, not part-sum), Q9 (delete racing completion, no deadlock, no negative counters), Q11 (`usage_sync` heals injected drift and reports it), Q12 (reservation survives a "restart"), Q14 (presign 403 / storage-rejected PUT / manifest 422 with no ledger change **and** the reservation released), Q15 (soft delete frees immediately, pending-deletion visible), Q16 (4.7/5 GiB: first tab capped at ~300 MiB, second blocked), the downgrade race at completion, pre-ledger sessions, the mirror-clobber case (guard and meters read the rows), and the switch semantics. Full regression **1,390 / 0 / 0**; client build; boot smoke. Docs: `16` §4.7, `08` §5 + §13, `10` §3, `23` Phase 3, `.env.example`. **Deferred, explicitly:** Q13 (worker failure mid-transcode — Phase 7 workers) and Q17's server half (recorder hits its ceiling mid-recording — recorder matrix R23; the client half is T-303's suite); the 30-day hard purge that drains `pending_deletion` (`maintenance.cleanup`, Phase 6); `POST /me/entitlements` (T-307 reads the legacy one). `QUOTA_ENFORCEMENT_V2` **stays OFF** until the ≥14-day notice has run.

**T-307 · Quota UX** — ✅ DONE (2026-09-11), autonomous. **Dual meters:** `client/src/lib/quotaMeters.js` normalizes the v1 `/api/v1/me/usage` body (else the legacy summary + plan) into `storage.display` / `videos.display` ("4.2 GB / 5 GB", "38 / 50"); `StorageMeter` + `VideosMeter` (+ `DualMeters`) each with their own bar; the sidebar and the billing page render **both, always** — the either/or sidebar and the cap-less "Videos recorded" line are gone; **no blended percentage anywhere** (asserted on the shape and every component); near-limit hint with the exact `16` §4.6 copy. **Recorder pre-flight** (`extension/quotaPreflight.js`, UMD): blocked → Start replaced by the exact block message + *Manage videos* / *Upgrade* / *Check again*; warn → the exact near-limit copy + "storage for about N more minutes at this quality" (bytes/s per quality from `16` §1.1) and recording proceeds; unknown → proceeds (the server enforces). Wired after the T-403 scan, before auto-start. **Finalize quota refusal:** Save to device / *Delete a video & retry* / Upgrade; **recovery card quota verdict:** Download / *Delete a video & retry* / Upgrade (`05` §6.1.3). Tests: `tests/quota-ux.test.js` (**44**: the normalizer from both sources, exact display strings, exact copy, no percentage field, assessment states, the pre-flight's blocked/warn/ok/unknown with the minutes estimate by quality, and the wiring). **Verified in the real client against the real server** (`QUOTA_ENFORCEMENT_V2=true`, an account seeded to 4.7 GiB / 46 videos): sidebar `Storage 4.7 GB / 5 GB` + `Videos 46 / 50` as two bars with no percentage in the text; billing page both meters, each with the exact near-limit copy; the pre-flight on the live account → `warn` with 10 / 16 / 37 minutes for high / medium / low. **Playwright not used** (not installed); the in-app browser and Node covered the same ground. **Deliberately NOT done:** the recorder's blocked/warn states could not be exercised inside the real extension window (chrome.* APIs) — covered by the pure module against the live endpoint and source-level wiring; no `UpgradeModal` changes (its meta contract is unchanged and already carries the real numbers).

## Phase 4 — Local recovery

**T-401 · RecorderStore (IndexedDB)** — ✅ DONE (2026-09-11), autonomous. `extension/recorderStore.js` (**UMD, not `store/recorderStore.ts`** — no TypeScript toolchain in the extension, per T-303), global `VeoRecRecorderStore`. `veorec-recorder` v1 with `sessions` (`id`), `chunks` (`[sessionId, seq]`), `parts` (`[sessionId, partNumber]`); `navigator.storage.persist()` requested best-effort with the grant logged; `checkSpace()` per `05` §4 (`ok` / `warn` < 2 GB / `insufficient_disk` < 500 MB; unknown or failing estimates never block). Sessions carry the `05` §3 fields incl. `userId` and the `updatedAt` heartbeat; `liveness()` = live within 15 s else dead. **Chunk writes are queued per session so `seq` is assigned in arrival order even when the recorder never awaits** (asserted with 100 un-awaited concurrent appends); `chunkCount`/`totalBytes` maintained in the same transaction; one retry then `persist_failed`; `QuotaExceededError` → `quota_exceeded` with no pointless retry; the queue survives failures. Parts: pending → inflight → uploaded with etag; **an upsert that omits `status` keeps the stored one** (a real defect the suite caught: bumping `attempts` used to demote an uploaded part to pending, which also made pruning silently do nothing). Pruning: nothing unless forced, then only chunks covered by `uploaded` parts **with** an etag. `deleteSession` removes session + chunks + parts in one transaction; `gc()` removes sessions untouched for 7 days with a console notice. A second real defect: the per-session queue's housekeeping promise re-surfaced a failed write as an unhandled rejection (a page-crashing bug in a browser, a process-killing one in Node); fixed. Tests: `tests/recorder-store.test.js` (**64 assertions**, real IndexedDB semantics via `fake-indexeddb` — a `server/` devDependency, the canonical test tool for this task). Docs: `05` §2.1, `04` §5.2. **Deliberately NOT done:** no recorder wiring (T-402), no recovery flow or UI (T-403), no `unlimitedStorage` manifest permission (`05` relies on `persist()` + space checks; revisit only if T-402's browser run shows the extension quota binding).

**T-402 · Chunk persistence wiring** — ✅ DONE (2026-09-11), autonomous. `recorder.html` loads `recorderStore.js` before `uploader.js`; the local session is created with the actual `MediaRecorder.mimeType` before the first chunk (owner from `sr_user`); **every `dataavailable` chunk is persisted FIRST — fire-and-ordered, never awaited — then pushed to the legacy array, then fed to the uploader** (asserted on source, in that order); 5 s heartbeat; server linkage written as soon as `begin()` returns; sealed parts recorded `pending` before their PUT with their chunk range derived from the fed-chunk sizes, etag recorded on upload; `PERSIST_FAILED` shown once, recording continues, quota pressure prunes only verified-part chunks; `stopped` → `uploading` at finalize; **deleted only after server-confirmed completion on both the v1 and the legacy path**; a failed upload keeps the session as `failed` for T-403; cancel/restart discard it and abort the server session best-effort; a zero-chunk take is deleted (no phantom recovery); under 500 MB free the recorder refuses to start, under 2 GB it warns (`05` §4). Tests: `tests/recorder-wiring.test.js` (**39**: load order, persist-before-everything, never awaited, banner-once, delete-after-complete on both paths, keep-on-failure, discard paths, part bookkeeping, every store call best-effort, and the helpers simulated against fake-indexeddb). **R12 partial verified in a real browser** against the real store file: 10 × 1 KiB chunks written as 1 s `dataavailable` slices, the page context destroyed and reloaded, then the session found (`recording`, still `live`), all 10 rows present and ordered, the assembled blob byte-identical (10,240 bytes), the part row and its etag intact. **Playwright specifically was NOT used** (not installed; ~300 MB of browsers) — the in-app browser covered the same ground. **Deliberately NOT done:** no launch scan, recovery card or server reconciliation (T-403); the legacy `chunks` array is still filled (the memory rule is the Phase 5 state machine); no `unlimitedStorage` permission.

**T-403 · Recovery flow + UI** — ✅ DONE (2026-09-11), autonomous. `extension/recovery.js` (UMD, no DOM, no `chrome.*`, deps injected) + recorder/popup wiring. **Launch scan before auto-start** (`03` §3.1): 7-day GC, dead zero-chunk phantoms deleted, a live heartbeat refuses to start ("already running in another window… wait 15 seconds", *Check again*), unfinished takes listed newest-first in a recovery card with **Resume upload** (own sessions only) / **Download** (`fixWebmDuration`) / **Discard** (confirm; server session aborted best-effort). **Reconciliation per `05` §6.1**: the uploader's `resume()` diff adopts server-held parts (server view wins, local rows corrected), a `completed` server session → *already saved* + local delete, expired/aborted/gone → fresh session, offline start → recording created first (idempotent), and **only chunks not covered by server-held parts are fed**. Outcomes keep the data on 401 (`auth_required` — R16), 403 quota (Download / delete-and-retry remain), any failure, or a cross-account session; local data is deleted only for a phantom, on server-confirmed completion, or on explicit discard (asserted). Popup badge "N unsaved recording(s) — open to recover", live-updated. **Recovery KPI emitting**: every recovery call is tagged `X-VeoRec-Recovery: 1`; the server counts attempts/successes/failures and reports `kpi_snapshot.recovery.effectivenessPct`. Tests: `tests/recovery.test.js` (**56**: every `05` §8 matrix row against fake-indexeddb with a scripted uploader — mid-recording crash with server-held parts (only the uncovered chunk fed), crash-after-complete (nothing re-uploaded), expired → fresh (stale local part not trusted), offline → recording created, 401/quota/failure/cross-account/network keep the data, download byte order + duration, discard aborts then deletes; recorder/popup wiring; the server KPI). **R12 / R13 / R22 verified end to end in a real browser** against the real server, PostgreSQL and MinIO: chunks persisted, page context destroyed, the session first reported `live` (heartbeat < 15 s — correct), then `dead` and recovered — recording created, uploaded, completed, `uploaded` with a signed playback URL, local session gone, both server KPI lines `recovery:true`. **Not verifiable here:** a real Chrome crash / extension reload (the harness destroys the page context, which is what IndexedDB survival depends on); Playwright not used. **Deliberately NOT done:** no recorder state machine (Phase 5); the legacy `chunks` array remains; the normal (non-recovery) local download still uses `lastBlob`.

## Phase 5 — State machine

**T-501 · RecorderMachine core** — ✅ DONE (2026-09-11), autonomous. `extension/machine.js` (UMD, pure, no DOM/chrome/MediaRecorder/network): the `03` §2 states and transitions plus the internal `finalizing`, the §3.6 stop guard (`STOP_TIMEOUT`, 10 s), the §7–§8 chunk/limit events and the §9 interruption events; every side effect is a named injected effect, never awaited, a missing one simply not requested, a throwing one logged; `recording` entry in the §3.4 order (session row → upload session → recorder → clock → wake lock → projection), resume does not re-run it; elapsed computed in one place; limits (duration + bytes) from the chunk clock with a re-arming backstop, once-only 30 s / 90 % warnings, `clientDuration` clamped; disposer registry with unregister closures, per-state disposal, `disposeAll` on idle/cancelling/terminal, `leaks()`; projection on every transition with legacy keys mirrored; queued dispatch (synchronous nested sends run after the current transition); unknown/invalid events ignored and logged. Dead defensive branches were removed rather than "covered". Tests: `tests/machine.test.js` (**428**: every state × every event asserted individually — 13 × 26 cells — plus entry order, elapsed/pause math, both limits with warnings and clamps, the lost-onstop guard, interruptions, cancel/restart, disposer leaks and idempotence, projection contract, re-entrancy, defaults, and the browser UMD branch via a vm sandbox). **100 % block coverage** of `machine.js` measured with V8 precise coverage (`tests/coverage-machine.js`, both load paths merged positionally). **Deliberately NOT done:** no capture effects (T-502), no recorder rewiring (T-503) — `recorder.js` is unchanged.

**T-502 · CaptureManager** — ✅ DONE (2026-09-11), autonomous. `extension/capture.js` (UMD, every browser dependency injectable): `constraintsFor` per mode/quality (tab keeps the REQUIRED legacy `mandatory` shape; screen/window `displaySurface` + `systemAudio:'include'`; camera-only 720p), `acquire` in order with every acquired track stopped on failure and a `CaptureError` carrying a `18` §4 code (`permission_dismissed` / `permission_denied` / `no_device` / `tab_capture_failed` / `constraint_failed` after **one automatic lower-quality retry** / `device_busy` / `capture_failed`); **mic denial throws `mic_denied`** — never a silent mic-less take, "Record without mic" = `acquire({audio:false})`; the mixer with per-source gain, tab loopback only in tab mode, a mic counted only if it carries audio, the suspended-context safeguard (resume → ≤ 1.5 s wait → `audio_context_suspended` warning rather than silence), an `onstatechange` re-resume and `dropMic`; the legacy warning texts verbatim; `recorderOptions` with the vp9,opus → vp8,opus → webm chain, quality bitrate and `audioBitsPerSecond: 128 000`; `watch` for `TRACK_ENDED {video|mic|tab_audio}` and `MIC_LOST` on `devicechange` (no auto-switch), returning a disposer. **A real defect found by the suite and fixed:** a mic stream carrying no audio track was counted as a source, so `no_audio_at_all` could never fire. Tests: `tests/capture.test.js` (**66**, scripted fakes reproducing the browser contracts): R1–R6 modes and warnings, R7 mic-denied policy and the muted retry, R8 picker cancelled, R10 stop-sharing bar, R20 mic unplugged (dropped from the mix, recording continues), every `18` §4 code, the single constraint retry, the suspended-context paths, devicechange with/without the mic, camera failure, disposal, boundaries. **Real-browser smoke** (in-app browser, no picker): the real `isTypeSupported` chain chose `vp9,opus`; a real `AudioContext` mixer fed by oscillator streams produced a live mixed track (running, not suspended); a real `MediaRecorder` recorded 7 chunks / 31,805 bytes from it; `dropMic`/`dispose` correct. **Not verifiable here:** `getDisplayMedia`/`getUserMedia` with a real picker or real devices (needs Playwright fake-media, not installed) — R9/R11 belong to the machine (T-501) and the quota work (T-306/T-307). **Deliberately NOT done:** no recorder wiring (T-503) — `recorder.js` is unchanged.

**T-503 · Recorder UI refactor** — recorder.html renders machine projections; overlay/popup consume `recSession` (legacy keys mirrored); mic-denied choice UI; upload progress UI. Deps: T-501, T-502, T-403. Tests: extension Playwright suite. Accept: full matrix (`20` §9) green; beta-channel soak 2 weeks.

## Phase 6 — Queue

**T-601 · BullMQ infra + outbox** — JobQueue interface, worker app scaffold, processing_jobs outbox relay, graceful shutdown, `/admin/jobs` list+retry. Tests: outbox crash-window test; stalled-job requeue. Accept: enqueue→process→row lifecycle observable.

**T-602 · Maintenance jobs** — usage_sync, subscription_sync, storage_verification, upload_expiry, session purge as repeatables; delete `server/cron.js`. Deps: T-601. Tests: each job idempotent on fixtures. Accept: cron.js gone; jobs visible in admin.

**T-603 · Transcription as job** — Relocate `transcription.js`/`ai.js` into worker; `stt.transcribe` + transcripts tables + status endpoints; API transcribe endpoints → 202. Deps: T-601. Tests: restart-safety (kill worker mid-job); Groq mock w/ 429s; whisper.cpp fallback. Accept: no transcription runs in the API process.

## Phase 7 — Processing

**T-701 · Probe job** — Download, ffprobe, validation, facts write, post-probe entitlement, fan-out (`09` §2). Tests: fixture battery (`20` §8). Accept: corrupt fixture → failed(probe_invalid); valid → facts recorded.

**T-702 · Transcode MP4 + maybe_mark_ready** — `09` §3 + promotion logic. Deps: T-701. Tests: output verification, idempotent re-run, VFR fixture sync check. Accept: uploaded→ready pipeline works end-to-end in staging.

**T-703 · Thumbnails/poster/preview** — `09` §4 incl. play-overlay variant for Gmail. Deps: T-701. Accept: non-black poster on screen-recording fixtures.

**T-704 · Audio extract + captions VTT** — `09` §6–7; captions asset from segments. Deps: T-701, T-603. Accept: watch page `<track>` renders captions.

**T-705 · HLS job** — `09` §5, conditional. Deps: T-702. Accept: hls.js + Safari native playback of a 10-min fixture.

**T-706 · Legacy re-processing backfill** — Queue-fill probe/transcode/thumbnail for all recordings with R2 sources; progress dashboard. Deps: T-702, T-703, T-204. Accept: 100% active recordings have mp4+poster.

## Phase 8 — Watch

**T-801 · Watch/media/status endpoints** — `08` §7 + signed URL minting + privacy enforcement point (media-level). Deps: T-302, T-702. Tests: privacy × endpoint matrix; URL TTL behavior. Accept: no Cloudinary read in watch path (flagged clients).

**T-802 · Watch page refactor** — Decomposition (`11` §4), explicit page states, status polling, URL refresh, keyboard controls, alert() removal; delete 404-retry + 12s hacks (guard legacy). Deps: T-801. Tests: Playwright watch suite incl. processing state + error paths. Accept: Safari + mobile verified; player error rate < 0.5% after rollout.

**T-803 · Dashboard/folders/notifications on v1** — List endpoints on Postgres, notifications queries (`13` §6). Deps: T-302, T-105. Accept: meta.json/Cloudinary reads off for these pages.

## Phase 9–13 (task headlines; expand per phase kickoff)

**T-901** share_links CRUD + gate resolution; **T-902** signed download + audience.download enforcement; **T-903** unlisted default + migration notice.
**T-1001** engagement writes → Postgres only; **T-1002** analytics endpoints + retention decile; **T-1003** paywall events unified.
**T-1101** translation cache; **T-1102** auto-process chain as jobs incl. DEFAULT_TITLES guard; **T-1103** AI status surfaces + retry UI.
**T-1201** edit_sessions/ops endpoints + editor wiring; **T-1202** render job (single-source stream-copy path); **T-1203** multi-source compose render; **T-1204** silence-removal job (audio-based); **T-1205** delete replace/trim/compose legacy handlers (aliases → sessions).
**T-1301** billing_events ledger + webhook rewrite (dup/ordering/500s); **T-1302** sessions-based auth + revocation + bridge compat; **T-1303** subscriptions reads → Postgres only; **T-1304** rate limits → Redis.

## Phase 14

**T-1401** legacy upload route removal (fleet <1% + forced update); **T-1402** delete JSON store modules + Cloudinary SDK + dead client hacks; **T-1403** final import freeze + Cloudinary export & plan cancellation; **T-1404** docs cleanup (remove migration shims from specs).

---

## Ordering summary (critical path)

T-101→T-106 → T-201→T-204 → T-301→T-306 → T-401→T-403 (→ T-307) → T-501→T-503 → T-601→T-603 → T-701→T-706 → T-801→T-803 → then 9–13 in any order → 14.

## Definition of done per task

Code + tests listed + docs updated (if the task changes a contract, the spec doc changes in the same PR — rule from `26`) + observability (new component logs/metrics per `19`) + reviewed against the invariants checklist (`02` §11).
